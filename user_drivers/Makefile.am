SUBDIRS = $(USER_DRIVERS_SUBDIRECTORIES)
DIST_SUBDIRS = $(USER_DRIVERS_SUBDIRECTORIES)

# Extra actions for clean and distclean
distclean-local:
	rm -f *~
clean-local:
	rm -f *~

doc_dirs:
	echo $(SUBDIRS)
