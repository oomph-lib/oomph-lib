# Run analyse script during check
check_SCRIPTS = analyse.sh

TESTS = analyse.sh


# Include script into the distribution:
EXTRA_DIST = \
analyse.sh

# Tidy up:
clean-local:
	rm -f validation.log *~
dist-clean-local:
	rm -f validation.log *~

