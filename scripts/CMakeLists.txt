list(APPEND CMAKE_MESSAGE_INDENT " ")
message(VERBOSE "Entered bin subdirectory")

# Don't need to do anything; CPack will package everything up by default...

message(VERBOSE "Leaving bin subdirectory")
