bootstrap.min.js
jquery.powertip.min.js
jquery-1.12.0.min.js

all unchanged from their respective sources, boostrap.min.js was built from 
getbootstrap.com, and therefore only contains the packaged requires. See css 
folder for config.json file.


NOTE

The code:
$(".contents").addClass("container");
$(".header").addClass("container");
$(".navpath").addClass("container");

is found in the oomph-lib_footer.html.template, so that all references to 
.container also refer to .contents .header and .navpath 
