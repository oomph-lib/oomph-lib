repos:
  - repo: local
    hooks:
      - id: cmake-format
        name: cmake-format
        entry: cmake-format
        description: Format CMakeLists.txt files using cmake-format.
        args: ["--in-place"]
        language: python
        types: [cmake]
