# The src directories that will be included by make dist. Add directories
# here to include them in the resulting tar file.
DIST_SUBDIRS = $(SUBDIRS) jack_cool

# Extra actions for clean and distclean
distclean-local:
	rm -f *~
clean-local:
	rm -f *~
doc_dirs:
	echo $(SUBDIRS)
